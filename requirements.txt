# Web Framework
flask>=3.0.0
gunicorn>=21.0.0

# Databricks Connection
# databricks-connect is required for both:
# - Databricks Apps environment: Uses DatabricksSession with Service Principal OAuth
# - Local development: Uses DatabricksSession with PAT authentication
# It includes pyspark, so DO NOT install pyspark separately
databricks-connect>=14.3.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# WeCom Encryption Support
pycryptodome>=3.19.0

# Streamlit App
streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.18.0
