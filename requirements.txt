# Web Framework
flask>=3.0.0
gunicorn>=21.0.0

# Databricks Connection (for local development only)
# Note: In Databricks Apps environment, PySpark is already available natively
# databricks-connect is only needed for local development to connect to Databricks
# It includes pyspark, so DO NOT install pyspark separately
databricks-connect>=14.3.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Streamlit App
streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.18.0
